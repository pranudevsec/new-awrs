{
  "name": "award_panel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run watch --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run watch --prefix backend",
    "build": "concurrently \"npm run build --prefix backend\" \"npm run build --prefix frontend\"",
    "install:frontend": "npm install --prefix frontend",
    "install:backend": "npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
